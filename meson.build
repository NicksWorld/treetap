project('treetap', 'c',
  version: '0.0.0',
  license: 'ISC',
  default_options: [
    'warning_level=3',
    'c_std=c99',
  ],
  meson_version: '>= 1.1',
)

# Core library powering the treetap frontend
subdir('libtap')

# TODO: Implement the treetap frontend
# subdir('treetap')
